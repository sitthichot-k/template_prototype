# FRONTEND - PREPRODUCTION
# Served same-origin behind nginx, which proxies /api to the backend.
# Every value here is public - it ships inside the JavaScript bundle.

VITE_API_BASE_URL=
VITE_API_PREFIX=/api/v1

VITE_APP_NAME=MyApp
VITE_DEFAULT_LOCALE=en
